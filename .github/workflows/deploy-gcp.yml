name: Deploy to Google Cloud Run

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  REGION: asia-northeast1
  SERVICE_NAME: business-management

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify secrets
        run: |
          echo "Checking required secrets..."
          MISSING_SECRETS=()
          
          if [ -z "${{ secrets.GCP_SA_KEY }}" ]; then
            MISSING_SECRETS+=("GCP_SA_KEY")
          fi
          
          if [ -z "${{ secrets.GCP_PROJECT_ID }}" ]; then
            MISSING_SECRETS+=("GCP_PROJECT_ID")
          fi
          
          if [ ${#MISSING_SECRETS[@]} -gt 0 ]; then
            echo "::error::The following secrets are not set or are empty: ${MISSING_SECRETS[*]}"
            echo "Please set these secrets in GitHub repository settings: Settings > Secrets and variables > Actions"
            exit 1
          fi
          
          echo "✓ All required secrets are properly configured"
          
          # Supabase環境変数の確認
          if [ -z "${{ secrets.SUPABASE_URL }}" ] || [ -z "${{ secrets.SUPABASE_KEY }}" ]; then
            echo ""
            echo "⚠️  ⚠️  ⚠️  WARNING ⚠️  ⚠️  ⚠️"
            echo "⚠️  Supabase環境変数が設定されていません。"
            echo "⚠️  ローカルファイルストレージを使用しますが、"
            echo "⚠️  Cloud Runではデータは永続化されません。"
            echo "⚠️  データを永続化するには、Supabase環境変数を設定してください。"
            echo "⚠️  ⚠️  ⚠️  ⚠️  ⚠️  ⚠️  ⚠️  ⚠️  ⚠️  ⚠️"
            echo ""
          else
            echo "✓ Supabase環境変数が設定されています。"
          fi

      - name: Install Python dependencies for testing
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          if [ -n "$SUPABASE_URL" ] && [ -n "$SUPABASE_KEY" ]; then
            python3 -m pip install --upgrade pip
            pip install supabase>=2.0.0
          fi

      - name: Test Supabase connection
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: |
          if [ -n "$SUPABASE_URL" ] && [ -n "$SUPABASE_KEY" ]; then
            echo "Testing Supabase connection..."

            # Pythonスクリプトを実行
            python3 << 'EOF'
            import os
            import sys

            try:
                from supabase import create_client

                supabase_url = os.getenv('SUPABASE_URL')
                supabase_key = os.getenv('SUPABASE_KEY')

                if not supabase_url or not supabase_key:
                    print('❌ Supabase環境変数が設定されていません')
                    sys.exit(1)

                print(f'🔗 Supabase URL: {supabase_url[:50]}...')

                # クライアント作成
                client = create_client(supabase_url, supabase_key)
                print('✓ Supabaseクライアントを作成しました')

                # テーブルアクセステスト
                required_tables = [
                    'users_master',
                    'daily_reports',
                    'staff_accounts',
                    'morning_meetings',
                    'tags_master',
                    'daily_users'
                ]

                failed_tables = []
                for table_name in required_tables:
                    try:
                        response = client.table(table_name).select('id').limit(1).execute()
                        print(f'✓ {table_name}: アクセス可能')
                    except Exception as e:
                        error_msg = str(e)
                        print(f'❌ {table_name}: アクセスエラー - {error_msg[:100]}')
                        failed_tables.append(table_name)

                if failed_tables:
                    print('')
                    print('⚠️  以下のテーブルにアクセスできません:')
                    for table in failed_tables:
                        print(f'   - {table}')
                    print('')
                    print('💡 解決方法:')
                    print('   1. Supabase Dashboard → SQL Editor を開く')
                    print('   2. supabase_schema.sql の内容を実行してください')
                    print('   3. 特に、RLS (Row Level Security) が無効化されているか確認してください')
                    sys.exit(1)

                print('')
                print('✅ すべてのテーブルにアクセス可能です！')

            except ImportError:
                print('❌ supabaseパッケージがインストールされていません')
                print('   requirements.txtにsupabase>=2.0.0が含まれているか確認してください')
                sys.exit(1)
            except Exception as e:
                print(f'❌ Supabase接続エラー: {e}')
                print('')
                print('💡 トラブルシューティング:')
                print('   1. SUPABASE_URLとSUPABASE_KEYが正しいか確認してください')
                print('   2. Supabaseプロジェクトが有効か確認してください')
                print('   3. APIキーに適切な権限があるか確認してください')
                sys.exit(1)
EOF

            # Pythonスクリプトの終了コードをチェック
            if [ $? -ne 0 ]; then
              echo "::error::Supabase接続テストに失敗しました。デプロイを続行しますが、アプリケーションが正常に動作しない可能性があります。"
              echo "::warning::Supabase設定を確認してください。詳細は上記のエラーメッセージを参照してください。"
            fi
          else
            echo "Supabase credentials not set, skipping connection test"
          fi

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT_ID }}

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker asia-northeast1-docker.pkg.dev

      - name: Build and push Docker image
        run: |
          docker build -t asia-northeast1-docker.pkg.dev/$PROJECT_ID/business-management/app:${{ github.sha }} .
          docker push asia-northeast1-docker.pkg.dev/$PROJECT_ID/business-management/app:${{ github.sha }}

      - name: Deploy to Cloud Run
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          GROK_API_KEY: ${{ secrets.GROK_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # 環境変数の構築（空の値は除外）
          ENV_VARS=""
          
          # Supabase環境変数（設定されている場合のみ追加）
          if [ -n "${SUPABASE_URL}" ] && [ -n "${SUPABASE_KEY}" ]; then
            ENV_VARS="SUPABASE_URL=${SUPABASE_URL},SUPABASE_KEY=${SUPABASE_KEY}"
          fi
          
          # APIキー（設定されている場合のみ追加）
          if [ -n "${GROK_API_KEY}" ]; then
            if [ -n "${ENV_VARS}" ]; then
              ENV_VARS="${ENV_VARS},GROK_API_KEY=${GROK_API_KEY}"
            else
              ENV_VARS="GROK_API_KEY=${GROK_API_KEY}"
            fi
          fi
          if [ -n "${GEMINI_API_KEY}" ]; then
            if [ -n "${ENV_VARS}" ]; then
              ENV_VARS="${ENV_VARS},GEMINI_API_KEY=${GEMINI_API_KEY}"
            else
              ENV_VARS="GEMINI_API_KEY=${GEMINI_API_KEY}"
            fi
          fi
          
          echo "Setting environment variables..."
          if [ -n "${ENV_VARS}" ]; then
            gcloud run deploy $SERVICE_NAME \
              --image asia-northeast1-docker.pkg.dev/$PROJECT_ID/business-management/app:${{ github.sha }} \
              --region $REGION \
              --platform managed \
              --allow-unauthenticated \
              --set-env-vars "$ENV_VARS"
          else
            gcloud run deploy $SERVICE_NAME \
              --image asia-northeast1-docker.pkg.dev/$PROJECT_ID/business-management/app:${{ github.sha }} \
              --region $REGION \
              --platform managed \
              --allow-unauthenticated
          fi

      - name: Show deployment URL
        run: |
          echo "Deployment completed!"
          gcloud run services describe $SERVICE_NAME --region $REGION --format 'value(status.url)'

